/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export', // Uncomment if I want to export a fully static site and disable server-side rendering
    reactStrictMode: true,
    swcMinify: true,
};

module.exports = nextConfig;
